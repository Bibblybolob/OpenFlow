{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Hub and Flow Bar windows",
  "windows": ["hub", "flowbar"],
  "permissions": ["core:default", "opener:default", "updater:default"]
}
